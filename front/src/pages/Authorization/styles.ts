import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  root: {
    display: "flex",
    margin: "auto",
    flexDirection: "column",
  },
  btns: {
    display: "flex",
    gap: "12px",
  },
  btn: {},
}));
